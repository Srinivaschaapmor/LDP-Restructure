# 02 · Architecture

Populated in the **Architecture** phase: system context, app structure (App Router),
rendering strategy (SSR/SSG/ISR), data flow, folder conventions, and diagrams.

- `code-notes.md` — component/module rationale relocated out of inline code comments
  (see [project-coding-standards]).
